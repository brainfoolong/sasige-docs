language: php
php:
  - '5.6'
script: php -d short_open_tag=1 -f sasige/bin/sasige.php build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: public
  on:
    branch: master